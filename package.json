{
  "name": "lista-tarefas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server":"json-server db.json -w --port 3000"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
